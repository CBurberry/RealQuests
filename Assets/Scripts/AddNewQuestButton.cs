using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class AddNewQuestButton : MonoBehaviour
{
    public void OnClick()
    {
        //TODO - Show UI for creating a new quest (e.g. a form / panel)
        Debug.LogWarning(nameof(AddNewQuestButton) + ":" + nameof(OnClick));
    }
}
